// Este archivo se mantiene como marcador de compatibilidad mínima durante la retirada
// completa del modelo `Tarea`. No debe usarse para nueva lógica.

@Deprecated('Usa Evento en su lugar. Este modelo será eliminado en futuras versiones.')
class Tarea {
  // Placeholder: the real model was removed. Use `Evento` with `isTask = true` instead.
  const Tarea();
}
